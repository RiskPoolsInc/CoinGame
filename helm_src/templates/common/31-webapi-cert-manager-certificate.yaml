{{ if not .Values.ingress.existingTLSSecret }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api.{{.Values.ingress.hostname }}
spec:
  commonName: api.{{.Values.ingress.hostname }}
  dnsNames:
    - api.{{.Values.ingress.hostname }}
  duration: 2160h0m0s
  issuerRef:
    kind: Issuer
    name: letsencrypt-webapi-coingame
  privateKey:
    algorithm: RSA
    encoding: PKCS1
    size: 2048
  renewBefore: 120h0m0s
  secretName: coingame-api-ingress-tls-cert
  subject:
    organizations:
      - {{ .Values.certificate.organization }}
  usages:
    - server auth
    - client auth
status: {}
{{ end }}
