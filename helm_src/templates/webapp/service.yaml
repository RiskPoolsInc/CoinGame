apiVersion: v1
kind: Service
metadata:
  labels:
    com.ubix.network: {{ .Values.webapp.name }} 
  name: {{ .Values.webapp.name }}
spec:
  type: {{ .Values.webapp.service.type }}
  {{- if .Values.webapp.service.ports }}
  ports: {{- toYaml .Values.webapp.service.ports | nindent 4 }}
  {{- end }}
  selector:
    com.ubix.network: {{ .Values.webapp.name }}
status:
  loadBalancer: {}
